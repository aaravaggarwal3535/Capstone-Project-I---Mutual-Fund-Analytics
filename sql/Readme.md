# SQL Directory
Contains the database architecture definition and analytical query templates.

- `schema.sql`: Defines the Star Schema (fact and dimension tables).
- `schema_extra.sql`: Defines supporting indexes and tables.
- `queries.sql`: Pre-defined analytical queries used for report generation.