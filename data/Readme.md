# Data Directory
Contains the raw inputs and processed outputs for the mutual fund analytics pipeline.

- `raw/`: Contains original, unmodified CSV files (NAV history, transactions, etc.).
- `processed/`: Contains cleaned, transformed datasets ready for dashboard ingestion.